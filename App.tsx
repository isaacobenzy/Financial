/**
 * Legacy React Navigation entry — unused.
 * Boot path: expo-router via package.json `"main": "expo-router/entry"`.
 * Routes live under `app/`.
 */
export default function App() {
  return null;
}
